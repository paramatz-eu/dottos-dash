# The recommended first build. GPIO27 is safe to leave unconnected until you
# add a passive piezo buzzer.

substitutions:
  device_name: dottos-dash

packages:
  common: !include dottos-dash-common.yaml

output:
  - platform: ledc
    id: piezo_output
    pin: GPIO27

rtttl:
  output: piezo_output
